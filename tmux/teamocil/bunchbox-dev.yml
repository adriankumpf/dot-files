name: 'bunchbox-dev'
windows:
    - name: 'editor'
      root: '~/dev/work/bunchbox-app/'
      panes:
        - nvim

    - name: 'processes'
      layout: main-vertical
      root: '~/dev/work/bunchbox-app/'
      panes:
        - make start local api app worker collector demo
        - docker-compose up -d

    - name: 'git'
      root: '~/dev/work/bunchbox-app/'
      panes:
        - git status
